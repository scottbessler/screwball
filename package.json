{
  "name": "screwball-web",
  "version": "0.1.0",
  "private": true,
  "description": "Frontend lint tooling for the Screwball board island.",
  "scripts": {
    "lint": "oxlint --deny-warnings",
    "lint:fix": "oxlint --fix"
  },
  "devDependencies": {
    "oxlint": "^1.71.0"
  }
}
